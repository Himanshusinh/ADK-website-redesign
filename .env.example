# Inquiry form email delivery (used by /api/inquiry).
# Without these, the form falls back to opening the visitor's email app.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
INQUIRY_TO=inquiry1@adkeng.com
